<ul class={{this.styleNamespace}}>
  {{#each @movies as |movie|}}
    {{! using @ for load movies since passed from parent }}
    <MovieList::MovieListItem @movie={{movie}} @loadMovies={{@loadMovies}} />

    <hr />
  {{/each}}
</ul>